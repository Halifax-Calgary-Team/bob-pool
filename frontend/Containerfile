FROM node:18-alpine

WORKDIR /usr/src
COPY package*.json ./
RUN npm ci

# Copy source files
# COPY . . not necessary - compose mounts a volume here

# Reinstall dependencies after copying to ensure they're in the final layer
# This ensures node_modules is available even with volume mounts
# RUN npm install --legacy-peer-deps

# Copy entrypoint script
# COPY podman-entrypoint.sh /usr/local/bin/
# RUN chmod +x /usr/local/bin/podman-entrypoint.sh

# Expose port
# EXPOSE 3000 controlled by compose

# Use entrypoint to ensure dependencies on startup
# ENTRYPOINT ["podman-entrypoint.sh"]

# Start Vite dev server with host flag to allow external connections
# CMD ["npm", "run", "dev", "--", "--host"]

# Made with Bob
